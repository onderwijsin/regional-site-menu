diff --git a/dist/module/runtime/server/utils/media/ipx.js b/dist/module/runtime/server/utils/media/ipx.js
index 6e3ee03dafa7278ab72bd1adfb3d3d9bf8addd04..696e5a02cdd8322d289ff2a63bd7c5f4dd79a360 100644
--- a/dist/module/runtime/server/utils/media/ipx.js
+++ b/dist/module/runtime/server/utils/media/ipx.js
@@ -1,5 +1,4 @@
 import { createError } from "h3";
-import { createIPX, ipxFSStorage, ipxHttpStorage } from "ipx";
 import { readFile } from "node:fs/promises";
 import { extname, resolve } from "node:path";
 import { hasProtocol, parseURL } from "ufo";
@@ -20,18 +19,15 @@ export function requireAllowedDomain(id) {
 }
 export function getIpx(domain) {
   if (!cachedIpx) {
-    if (mediaConfig.external) {
-      cachedIpx = createIPX({
-        storage: {},
-        httpStorage: ipxHttpStorage({ domains: domain ? [domain] : [] }),
-        maxAge: DAY_IN_SECONDS
-      });
-    } else {
-      cachedIpx = createIPX({
-        storage: ipxFSStorage({ dir: publicDir }),
-        maxAge: DAY_IN_SECONDS
-      });
-    }
+    cachedIpx = () => ({
+      process: async () => {
+        throw createError({
+          statusCode: 501,
+          statusMessage: "IPX_UNAVAILABLE",
+          message: "Studio IPX transforms are disabled in this runtime."
+        });
+      }
+    });
   }
   return cachedIpx;
 }
